apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
  source:
    repoURL: https://github.com/your-user/your-repo.git
    path: nginx
